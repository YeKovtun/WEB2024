<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Готуємо Разом</title>
</head>
<body>
    <header>
        <h1>Готуємо разом</h1>
        <header>
            <nav>
                <h1><a href="/create">Додати рецепт</a></h1>
                <h1><a href="/about">Про кухаря</a></h1>
                <h1><a href="/privacy">Загальні</a></h1>
            </nav>
        </header>        
    </header>
    <main>
        <% if (reviews.length > 0) { %>
            <% reviews.forEach(review => { %>
                <article>
                    <h2><a href="/item/<%= review._id %>"><%= review.title %></a></h2>
                    <p><%= review.content.slice(0, 100) %>...</p>
                    <small>Дата: <%= review.date.toLocaleDateString() %></small>
                </article>
            <% }) %>
        <% } else { %>
            <p>Упс.. Рецептів ще немає</p>
        <% } %>
    </main>
</body>
</html>
